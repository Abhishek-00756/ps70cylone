// Runtime config for the static frontend (no build step / bundler involved).
// Change this if the backend runs on a different host/port, or set it from
// a server-injected value in a real deployment.
window.CYCLONEAI_API_BASE_URL = "http://localhost:8000";
